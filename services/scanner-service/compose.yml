services:
  scanner-service-migrate:
    build:
      context: ../..
      dockerfile: services/scanner-service/Dockerfile
      args:
        TARGET: ./cmd/migrate
    env_file:
      - ../../deployments/docker/env/common.env.example
      - .env.example
    depends_on:
      vector:
        condition: service_started
      postgres-scanner:
        condition: service_healthy

  scanner-service:
    build:
      context: ../..
      dockerfile: services/scanner-service/Dockerfile
    env_file:
      - ../../deployments/docker/env/common.env.example
      - .env.example
    ports:
      - "8081:8081"
    depends_on:
      vector:
        condition: service_started
      kafka-topics-init:
        condition: service_completed_successfully
      postgres-scanner:
        condition: service_healthy
      scanner-service-migrate:
        condition: service_completed_successfully
      redis:
        condition: service_healthy

  scanner-service-outbox-relay:
    build:
      context: ../..
      dockerfile: services/scanner-service/Dockerfile
      args:
        TARGET: ./cmd/outbox-relay
    env_file:
      - ../../deployments/docker/env/common.env.example
      - .env.example
    depends_on:
      vector:
        condition: service_started
      kafka-topics-init:
        condition: service_completed_successfully
      postgres-scanner:
        condition: service_healthy
      scanner-service-migrate:
        condition: service_completed_successfully
